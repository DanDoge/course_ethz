linear(or not) combination of Gaussian vector
- X + YZ / sqrt(1 + Z^2), consider the CDF?
  - condition on Z -> is Gaussian
  - integrate over Z
- somewhere in the slides we assumed the prior sigma = 1